name: Apply Hijri Date Fixes

on:
  push:
    branches: [fix/platform-audit-core]
    paths:
      - '.github/workflows/apply-hijri-date-fixes.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/platform-audit-core
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: node scripts/apply-hijri-date-fixes.mjs
      - run: npx prisma generate
      - run: node --check src/routes/site-inspections.routes.js
      - run: node --check src/routes/assets.routes.js
      - run: node --check src/routes/asset-cycles.routes.js
      - name: Remove temporary patch files
        run: rm -f scripts/apply-hijri-date-fixes.mjs .github/workflows/apply-hijri-date-fixes.yml
      - name: Commit verified fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/routes/site-inspections.routes.js src/routes/assets.routes.js src/routes/asset-cycles.routes.js scripts/apply-hijri-date-fixes.mjs .github/workflows/apply-hijri-date-fixes.yml
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Correct Hijri dates across inspections and assets"
            git push origin fix/platform-audit-core
          fi
