name: Apply Accounting Transformation Committee Backend

on:
  push:
    branches: [main]
    paths:
      - 'scripts/apply-accounting-transformation.cjs'
      - '.github/workflows/apply-accounting-transformation.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: node scripts/apply-accounting-transformation.cjs
      - run: npx prisma format
      - run: npx prisma generate
      - run: node --check src/config/accountingTransformation.js
      - run: node --check src/routes/accounting-transformation.routes.js
      - run: node --check src/app.js
      - name: Commit backend integration
        run: |
          if git diff --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add prisma/schema.prisma src/app.js
          git commit -m "Integrate accounting transformation committee backend"
          git push
