# Hierarchical Accounting Groups API

The hierarchy endpoint groups accounting transformation records by source/type, main classification, sub-classification, and accounting group. Summary metrics are returned first; leaf records are paginated and loaded only when requested.
