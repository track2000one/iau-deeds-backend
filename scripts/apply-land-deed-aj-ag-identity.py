from pathlib import Path


def replace_once(path, old, new):
    p = Path(path)
    text = p.read_text(encoding='utf-8')
    if old not in text:
        raise SystemExit(f'Expected block not found in {path}: {old[:120]!r}')
    p.write_text(text.replace(old, new, 1), encoding='utf-8')


service = 'src/services/accountingCycles.service.js'
replace_once(service, """const normalizeIdentityPart = (value) => {\n  const normalized = normalizeKeyPart(value);\n  return normalized && !UNAVAILABLE_IDENTITY_VALUES.has(normalized) ? normalized : '';\n};\n""", """const normalizeIdentityPart = (value) => {\n  const normalized = normalizeKeyPart(value);\n  return normalized && !UNAVAILABLE_IDENTITY_VALUES.has(normalized) ? normalized : '';\n};\n\nexport const LAND_DEED_NUMBER_COLUMN = 'AJ';\nexport const LAND_DEED_DATE_COLUMN = 'AG';\n\nconst normalizeArabicDigits = (value) => String(value ?? '')\n  .replace(/[٠-٩]/g, (digit) => String('٠١٢٣٤٥٦٧٨٩'.indexOf(digit)))\n  .replace(/[۰-۹]/g, (digit) => String('۰۱۲۳۴۵۶۷۸۹'.indexOf(digit)));\n\nconst normalizeLandDeedDate = (value) => {\n  const raw = normalizeArabicDigits(normalizeAccountingText(value));\n  if (!raw) return '';\n  const parts = raw.replace(/[.\\-]/g, '/').split('/').map((part) => part.trim()).filter(Boolean);\n  if (parts.length === 3 && parts.every((part) => /^\\d+$/.test(part))) {\n    return parts.map((part) => String(Number(part))).join('-');\n  }\n  return normalizeIdentityPart(raw);\n};\n\nexport const getAccountingLandDeedIdentity = (payload = {}) => {\n  const rawDeedNumber = normalizeArabicDigits(normalizeAccountingText(payload?.[LAND_DEED_NUMBER_COLUMN]));\n  const rawDeedDate = normalizeArabicDigits(normalizeAccountingText(payload?.[LAND_DEED_DATE_COLUMN]));\n  const deedNumber = normalizeIdentityPart(rawDeedNumber);\n  const deedDate = normalizeLandDeedDate(rawDeedDate);\n  return {\n    deedNumber,\n    deedDate,\n    complete: Boolean(deedNumber && deedDate),\n    rawDeedNumber: rawDeedNumber || null,\n    rawDeedDate: rawDeedDate || null,\n  };\n};\n""")

replace_once(service, """  const entityAsset = normalizeIdentityPart(core.entityAssetNumber);\n  const mof = normalizeIdentityPart(core.mofAssetNumber);\n\n  if (recordType === 'fixed_asset') {\n""", """  const entityAsset = normalizeIdentityPart(core.entityAssetNumber);\n  const mof = normalizeIdentityPart(core.mofAssetNumber);\n\n  // Land identity is governed by the deed itself. AJ = deed number and AG = deed date.\n  // This must be evaluated before legacy MOF/entity numbers so distinct deeds cannot\n  // collapse into one accounting row merely because another administrative number repeats.\n  if (recordType === 'land') {\n    const deed = getAccountingLandDeedIdentity(payload);\n    if (deed.complete) return `land:deed:${deed.deedNumber}:${deed.deedDate}`;\n  }\n\n  if (recordType === 'fixed_asset') {\n""")

replace_once(service, """export const buildAccountingSnapshotData = (input, authUser, extra = {}) => {\n""", """export const getAccountingRecordMatchKey = (record = {}) => {\n  const generated = createAccountingStableKey(record.recordType, record.payload || {});\n  return canonicalizeAccountingStableKey(generated || record.stableKey);\n};\n\nexport const buildAccountingSnapshotData = (input, authUser, extra = {}) => {\n""")

replace_once(service, """export const getCurrentAccountingCycle = () =>\n""", """export const synchronizeAccountingLandStableKeys = async (client = prisma) => {\n  const records = await client.accountingTransformationRecord.findMany({\n    where: { recordType: 'land' },\n    select: { id: true, stableKey: true, payload: true },\n  });\n  let updated = 0;\n  for (const record of records) {\n    const deed = getAccountingLandDeedIdentity(record.payload || {});\n    if (!deed.complete) continue;\n    const expected = createAccountingStableKey('land', record.payload || {});\n    if (!expected || expected === record.stableKey) continue;\n    await client.accountingTransformationRecord.update({\n      where: { id: record.id },\n      data: { stableKey: expected },\n    });\n    updated += 1;\n  }\n  return updated;\n};\n\nexport const getCurrentAccountingCycle = () =>\n""")

replace_once(service, """  for (const record of legacyRecords) {\n    const payload = record.payload || {};\n    const stableKey = record.stableKey || createAccountingStableKey(record.recordType, payload, record);\n    const sourceFingerprint = record.sourceFingerprint || createAccountingFingerprint(record.recordType, payload);\n    await prisma.accountingTransformationRecord.update({\n      where: { id: record.id },\n      data: {\n        cycleId: current.id,\n        stableKey,\n        sourceFingerprint,\n        changeType: record.changeType || 'baseline',\n      },\n    });\n  }\n\n  return current;\n};\n""", """  for (const record of legacyRecords) {\n    const payload = record.payload || {};\n    const stableKey = record.stableKey || createAccountingStableKey(record.recordType, payload, record);\n    const sourceFingerprint = record.sourceFingerprint || createAccountingFingerprint(record.recordType, payload);\n    await prisma.accountingTransformationRecord.update({\n      where: { id: record.id },\n      data: {\n        cycleId: current.id,\n        stableKey,\n        sourceFingerprint,\n        changeType: record.changeType || 'baseline',\n      },\n    });\n  }\n\n  // Re-key deed-bearing land records on startup without deleting or recreating rows.\n  // This keeps existing record IDs, attachments and audit history intact while making\n  // AJ + AG the authoritative matching identity for all current and historical cycles.\n  await synchronizeAccountingLandStableKeys(prisma);\n\n  return current;\n};\n""")

replace_once(service, """        id: true, stableKey: true, changeType: true, recordNumber: true, recordType: true,\n        entityName: true, entityAssetNumber: true, assetDescription: true,\n""", """        id: true, stableKey: true, changeType: true, recordNumber: true, recordType: true, payload: true,\n        entityName: true, entityAssetNumber: true, assetDescription: true,\n""")
replace_once(service, """            id: true, stableKey: true, recordNumber: true, recordType: true,\n            entityName: true, entityAssetNumber: true, assetDescription: true,\n""", """            id: true, stableKey: true, recordNumber: true, recordType: true, payload: true,\n            entityName: true, entityAssetNumber: true, assetDescription: true,\n""")
replace_once(service, """  const targetKeys = new Set(targetRecords.map((item) => canonicalizeAccountingStableKey(item.stableKey)).filter(Boolean));\n  const removedRecords = baseRecords.filter((item) => {\n    const key = canonicalizeAccountingStableKey(item.stableKey);\n""", """  const targetKeys = new Set(targetRecords.map((item) => getAccountingRecordMatchKey(item)).filter(Boolean));\n  const removedRecords = baseRecords.filter((item) => {\n    const key = getAccountingRecordMatchKey(item);\n""")

# Search by deed number/date in the generic accounting endpoint.
routes = 'src/routes/accounting-transformation.routes.js'
replace_once(routes, """    { region: { contains: search, mode: 'insensitive' } },\n  ] } : {};\n""", """    { region: { contains: search, mode: 'insensitive' } },\n    { payload: { path: ['AJ'], string_contains: search } },\n    { payload: { path: ['AG'], string_contains: search } },\n  ] } : {};\n""")

# Search by deed number/date in the hierarchy endpoint used by the records page.
hierarchy = 'src/routes/accounting-hierarchy.routes.js'
replace_once(hierarchy, """      { region: { contains: search, mode: 'insensitive' } },\n    ] }] : []),\n""", """      { region: { contains: search, mode: 'insensitive' } },\n      { payload: { path: ['AJ'], string_contains: search } },\n      { payload: { path: ['AG'], string_contains: search } },\n    ] }] : []),\n""")

# Treat AJ+AG as a strong land identity during cycle review when present.
review = 'src/routes/accounting-cycle-review-policy.routes.js'
replace_once(review, """  if (record.recordType === 'fixed_asset') {\n    return hasValue(payload.Y) || hasValue(payload.Z) || hasValue(payload.AB)\n      || hasValue(record.mofAssetNumber) || hasValue(record.entityAssetNumber);\n  }\n  return hasValue(payload.D) || hasValue(payload.E)\n    || hasValue(record.mofAssetNumber) || hasValue(record.entityAssetNumber);\n};\n""", """  if (record.recordType === 'fixed_asset') {\n    return hasValue(payload.Y) || hasValue(payload.Z) || hasValue(payload.AB)\n      || hasValue(record.mofAssetNumber) || hasValue(record.entityAssetNumber);\n  }\n  if (record.recordType === 'land' && hasValue(payload.AJ) && hasValue(payload.AG)) return true;\n  return hasValue(payload.D) || hasValue(payload.E)\n    || hasValue(record.mofAssetNumber) || hasValue(record.entityAssetNumber);\n};\n""")

# Unit tests for the deed identity and compatibility migration.
Path('tests/accounting-land-deed-identity.test.js').write_text("""import test from 'node:test';\nimport assert from 'node:assert/strict';\nimport {\n  createAccountingStableKey,\n  getAccountingLandDeedIdentity,\n  getAccountingRecordMatchKey,\n  synchronizeAccountingLandStableKeys,\n} from '../src/services/accountingCycles.service.js';\n\nconst base = { B: 'جامعة الإمام عبدالرحمن بن فيصل', C: '0029', D: '', E: 'legacy-asset', G: 'أرض جامعية' };\n\ntest('land stable key is deed number AJ plus deed date AG', () => {\n  const payload = { ...base, AJ: '360607002075', AG: '10/3/1398' };\n  assert.equal(createAccountingStableKey('land', payload), 'land:deed:360607002075:10-3-1398');\n});\n\ntest('same deed is stable even when legacy asset number changes', () => {\n  const first = { ...base, E: 'A-1', AJ: '330115016381', AG: '29/8/1442' };\n  const second = { ...base, E: 'A-99', AJ: '330115016381', AG: '29/08/1442' };\n  assert.equal(createAccountingStableKey('land', first), createAccountingStableKey('land', second));\n});\n\ntest('different deed number or deed date produces a different land identity', () => {\n  const first = { ...base, AJ: '330103025654', AG: '23/6/1440' };\n  const second = { ...base, AJ: '730103028454', AG: '18/5/1441' };\n  assert.notEqual(createAccountingStableKey('land', first), createAccountingStableKey('land', second));\n});\n\ntest('arabic digits and zero-padded deed dates normalize consistently', () => {\n  const latin = getAccountingLandDeedIdentity({ AJ: '517902005797', AG: '08/03/1398' });\n  const arabic = getAccountingLandDeedIdentity({ AJ: '٥١٧٩٠٢٠٠٥٧٩٧', AG: '٨/٣/١٣٩٨' });\n  assert.equal(latin.deedNumber, arabic.deedNumber);\n  assert.equal(latin.deedDate, arabic.deedDate);\n  assert.equal(latin.deedDate, '8-3-1398');\n});\n\ntest('missing AJ/AG falls back to the legacy identity path', () => {\n  const key = createAccountingStableKey('land', { ...base, AJ: 'Not Available', AG: 'Not Available' });\n  assert.match(key, /^land:entity:/);\n});\n\ntest('record matching recomputes AJ+AG even when stored stable key is legacy', () => {\n  const record = { recordType: 'land', stableKey: 'land:entity:0029:legacy-asset', payload: { ...base, AJ: '930105021011', AG: '22/9/1439' } };\n  assert.equal(getAccountingRecordMatchKey(record), 'land:deed:930105021011:22-9-1439');\n});\n\ntest('stable-key synchronization updates only deed-bearing land rows', async () => {\n  const updates = [];\n  const client = { accountingTransformationRecord: {\n    findMany: async () => [\n      { id: 'a', stableKey: 'land:entity:0029:1', payload: { ...base, AJ: '730105021013', AG: '22/9/1439' } },\n      { id: 'b', stableKey: 'land:entity:0029:2', payload: { ...base, AJ: 'Not Available', AG: 'Not Available' } },\n    ],\n    update: async (input) => { updates.push(input); return input; },\n  } };\n  const count = await synchronizeAccountingLandStableKeys(client);\n  assert.equal(count, 1);\n  assert.equal(updates[0].where.id, 'a');\n  assert.equal(updates[0].data.stableKey, 'land:deed:730105021013:22-9-1439');\n});\n""", encoding='utf-8')

print('Applied AJ/AG land deed identity changes.')
